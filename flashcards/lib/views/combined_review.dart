import 'package:flutter/material.dart';

class CombinedReview extends StatelessWidget {
  const CombinedReview({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
